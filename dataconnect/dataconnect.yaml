specVersion: "v1beta"
serviceId: "haelr"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "haelr-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
